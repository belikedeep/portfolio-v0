---
const allPosts = await Astro.glob("../pages/blog/*.md");
---

<ul>
  {
    allPosts.map((post) => (
      <li>
        <a href={post.url}>{post.frontmatter.title}</a>
      </li>
    ))
  }
</ul>
